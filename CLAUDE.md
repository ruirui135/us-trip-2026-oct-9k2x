# CLAUDE.md — アメリカ旅行しおり（2026年10月）引き継ぎ

> このフォルダは、岡崎さん（瑠威さん）＋同期2名（大野・千葉）の初の海外旅行（仙台→LA→アナハイム→ラスベガス→グランドキャニオン、7泊9日）の
> **共有しおりサイト**。GitHub Pagesで公開し、プランはGoogleスプレッドシートで全員編集→岡崎さん×Claudeが「再成形」して反映する“生きてるしおり”。

## このプロジェクト特有のルール（必ず守る）
- **★の日程詰め（時刻・段取り・移動手段）は相談しながら**。勝手に全部埋めない。
- **観光地は3人がある程度決めてからClaudeが精査**。こちらから先に観光プランを作り込まない。
- **グランドキャニオンのチケットはClaudeで決めない**（本人たちが決める）。
- **価格・気温・規則・営業情報などは必ずWeb調査＋出典URL**。古い可能性があるものは「要確認」と明記し断定しない。
- 共有サイト＆公開シート前提：**パスポート番号・カード番号・自宅住所などは載せない**。
- 本人の呼称は共有サイト上では「岡崎さん」に統一（生成時に「瑠威さん」→「岡崎さん」へ置換している）。

## 仕組み（アーキテクチャ）
- 公開サイトは**静的HTML**（データを焼き込み済み。ブラウザはライブ取得しない）。新しさは**再成形のたび**に更新。
- データの2層：
  - **A プラン**（日程・未決事項）＝Googleスプレッドシート（3人が編集）。
  - **B ガイド**（気になっていること回答・出典）＝Claude作、`_research.json` に保存。
- 再成形＝`build_site.py` が A（gviz CSV か xlsx）と B（_research.json）を読み、共通デザインで包んで `docs/` を再生成。

## ファイル地図
| ファイル | 役割 |
|---|---|
| `docs/index.html` | 公開本体（しおり）。**自動生成物・手で直さない** |
| `docs/how-to.html` | 参加者向け・スプシ編集のやり方。自動生成物 |
| `build_site.py` | **再成形エンジン**。これを実行すると docs/ を作り直す |
| `build_template.py` | スプシ用テンプレ `旅行データ_テンプレート.xlsx` を生成 |
| `_index_body.html` | index の本文テンプレ（`<!-- DAYS_SECTION -->` `<!-- TODO_SECTION -->` `<!-- GUIDE_SECTION -->` `{{UPDATED}}` のマーカーあり）。**デザインを直すならここ** |
| `_howto_body.html` | how-to の本文テンプレ |
| `_research.json` | ガイド「気になっていること」8件の調査結果（出典付き）。差し替え可 |
| `_proposals.json` | 「★段取りのたたき台」5件（空港/移動/GC/ディズニー/カジノ・出典＋decisions付き） |
| `旅行データ_テンプレート.xlsx` | スプシの種（つかいかた/日程/未決事項の3タブ） |
| `アメリカ旅行‗現状決まっている部分.docx` | 元メモ（原典） |

## 再成形のしかた（更新コマンド）
```
uv run --with openpyxl python build_site.py
```
- `build_site.py` 冒頭の `SHEET_ID` が**空ならローカル** `旅行データ_テンプレート.xlsx` を、**埋まっていればGoogleスプシ(gviz CSV)** を読む。
- 列は**見出し名**で対応付け、タブは**gid**で指定するため、列追加やタブ名変更に強い。

### スプシ接続（スプシを正データにするとき・1回だけ）
`build_site.py` 冒頭の `SHEET_ID` に、スプレッドシートURLの /d/ と /edit の間のIDを貼るだけ：
```python
SHEET_ID = "1ISMTDbcY3tUUZZ9R9tPcnsnIiHdFtEtfBlZF-JYJBKY"
```
タブ名「日程」「未決事項」で読む（gid不要・タブ名は変えない）。共有は「リンクを知っている全員：閲覧者」＋3人を「編集者」招待。
※スプシは既に公開読み取り確認済み（gviz CSV・2026-06-27）。**ただし今は日付/航空会社/★提案などの編集を手元xlsxに入れているため、先に最終xlsxをスプシへ再取込（File→Import→現在のスプレッドシートを置換）してから SHEET_ID を有効化すること。**

### ガイド（B）を増やす・直すとき
- 軽微：`_research.json` を直接編集 → `build_site.py` 実行。
- 本格的に再調査：トピックを決めて調査（出典必須・事実検証）→ `_research.json` の該当トピックを差し替え → 再成形。

## 公開（GitHub Pages）
- **リポジトリ**：https://github.com/ruirui135/us-trip-2026-oct-9k2x （main・/docs公開・初版push済み 2026-06-27）
- **公開URL（Pages有効化後）**：https://ruirui135.github.io/us-trip-2026-oct-9k2x/
- Pages設定：Settings → Pages → Deploy from a branch → main / `/docs`。
- **以後の更新**：`build_site.py` で再成形 → `git add . && git commit -m "更新" && git push`。
  - originは設定済み。pushは認証キャッシュで通る（2026-06-27に成功実績）。

## 進捗・残タスク
**済**：日付確定（10/8〜10/16）／往復United反映／ガイド「気になること」8件（出典付・荷物はUnited規格&洗濯1回）／★段取りのたたき台5件（提案＋3人で決めること付）／スプシ公開読取り確認。

**3人で決める（★→確定したらスプシ未決事項を更新→再成形）**
- [ ] Oogie Boogie Bash チケット（10/11日＝179ドル+税・別売・要早期購入）を3枚買うか
- [ ] アナハイム→ラスベガス移動（提案: FlixBus/Greyhound直行）
- [ ] グランドキャニオン行き方（提案: ウエストリムの日帰りバスツアー／サウスは飛行機ツアー。チケットは本人たち）
- [ ] 岡崎さん：Visa か Mastercard を1枚追加（Amexのみリスク・発行2〜3週間）

**運用**
- [ ] 最終xlsxをスプシへ再取込 → `build_site.py` の SHEET_ID 有効化（以後スプシが正）
- [ ] GitHub Pages 公開（git init→/docs公開）→ URLを3人へ
- [ ] 観光（フリーのLA等）は3人で決めてからClaudeが精査
- [ ] 出発前：ESTA・パスポート・各予約の最終確認

## 計画書
詳細計画：`C:\Users\komug\.claude\plans\claude-html-github-imperative-moth.md`
